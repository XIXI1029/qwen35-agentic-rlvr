# 实验报告（服务器复现版）

> 数据来源：服务器 `outputs_server/results/*.json`、`code_rl/code.log`。
> 环境：V100 32GB（Tesla PG503-216, sm_70）· torch 2.6.0+cu124 · trl 1.12 · transformers 5.16

## 1. 产物清单

| 产物 | 说明 |
|------|------|
| `outputs/qwen3.5-4b-sft` | 冷启动 SFT（GSM8K CoT，2500 条）|
| `outputs/qwen3.5-4b-grpo` | **数学 RLVR**（GSM8K 题库，与 SFT 不相交）|
| `outputs/qwen3.5-4b-grpo-code` | **代码 RLVR**（MBPP 464 题，300 步 / 337.9 分钟）|
| `outputs/outputs_server/results/` | 3 任务 × 4 模型的评估 JSON + SUMMARY.md |

## 2. 结果总表

**GSM8K（test，随机 50 题，贪心）**

| 模型 | 正确率 | 答对/50 |
|------|--------|---------|
| Qwen3.5-4B-Base | 30.0% | 15 |
| 4B-SFT（冷启动） | 34.0% | 17 |
| 4B-GRPO（数学 RLVR） | 34.0% | 17 |
| **Qwen3.5-9B-Base（参照）** | **40.0%** | **20** |

**HumanEval（164 题，pass@1）**

| 模型 | pass@1 | 通过/164 |
|------|--------|----------|
| Qwen3.5-4B-Base | 14.6% | 24 |
| 4B-SFT（**数学**冷启动） | 22.0% | 36 |
| 4B-GRPO-Code（代码 RLVR） | 22.0% | 36 |

**AIME2024（2024 年 60 题）**

| 模型 | 正确率 |
|------|--------|
| 4B-Base | 1.7%（1/60）|
| 4B-SFT / 4B-GRPO / 9B-Base | 0%（0/60）|

## 3. 结论

### ✅ 站得住的结论
1. **两套可验证任务的 RLVR 流水线全部跑通**（数字比对 / 沙箱跑测试），代码全部复用，证明**方法可迁移**。
2. **4B-GRPO 达 9B 的 85%**（GSM8K：34.0% vs 40.0%）——"小模型对齐大模型"的量化证据。
3. **数学冷启动 SFT 迁移到代码任务有显著收益**：HumanEval 14.6% → 22.0%（**+7.3pt**，n=164，超出噪声范围）。说明 SFT 教会的"分步推理/结构化输出"是**跨任务通用能力**，这是本次最漂亮的正向发现。
4. 工程资产：12GB/32GB 均可复现、受限代码沙箱、断点续训、自动下载与缓存治理。

### ⚠️ 必须如实说明的结论
5. **GRPO 的净收益 ≈ 0（两个任务都是）**：
   - GSM8K：SFT 34.0% → GRPO 34.0%（17→17）
   - HumanEval：SFT 22.0% → Code-GRPO 22.0%（36→36）
   - **但模型确实变了**：逐题对比 GSM8K 有 5 题"由错变对"、5 题"由对变错"；HumanEval 各有 3 题翻转。→ 不是加载错权重，而是 **RL 让策略发生了移动，却没有净提升**。
6. **训练内指标显示"在学但没泛化"**：代码 RL 训练池通过率从 11.2% → 15.1%（300 次采样统计），但 HumanEval（held-out）无提升。
7. AIME2024 对 4B/9B 都太难（0~1 题），**无区分度**，不能作为结论依据。

## 4. 归因分析（为什么净收益为 0）

| 原因 | 证据 | 影响 |
|------|------|------|
| **组内奖励方差为 0 的比例高** | 本地实测 `frac_reward_zero_std=0.5`；组大小仅 4 | 一半的步没有梯度信号，等价于只训了一半 |
| 步数偏少 | 数学 600 步 / 代码 300 步 | RL 明显欠训练 |
| 奖励过稀疏 | 纯 0/1，无部分分（config 里的 format/长度权重点未接线）| 学习信号弱 |
| 超参保守 | lr=1e-6 + KL beta=0.04（参考模型=SFT）| 策略被 KL 拉住，动得小 |
| 评测样本少 | GSM8K n=50 → 1σ≈6.7pt | 小幅变化无法分辨 |

## 5. 下一步改进（按性价比排序）

1. **组大小 `num_generations: 4 → 8/16`**（最关键）：降低零方差组比例，让每步都有优势信号。
2. **加部分分奖励**：格式奖励（是否给出最终答案标记）+ 长度惩罚，接上 `grpo_config.yaml` 里已有的权重位。
3. **加大步数与学习率**：`max_steps 600→2000+`，`lr 1e-6→2e-6`，`beta 0.04→0.02`。
4. **评测扩样**：GSM8K 全量 test（1319 题）或至少 200 题，提升分辨率。
5. 代码任务：训练分布（MBPP）与评测（HumanEval）差异大，若要提升 HumanEval，建议扩充同风格训练题（注意与评测集不重叠）。

## 6. 结论口径（可声明 / 不声明）

**✅ 有证据、可对外声明**
- 在 4B 上复现 GRPO + RLVR 流水线，覆盖**数学（答案比对）与代码（沙箱执行）两类可验证任务**
- 数学冷启动 SFT 带来跨任务迁移：HumanEval **14.6% → 21.9%（+7.3pt）**
- GSM8K 上 v1 达 9B 的 85%；**v2 达 42%（>9B 的 40%）**
- **v2 改进（组大小 8 + 部分分奖励）使两任务都出现净提升**：HumanEval +3.1pt、GSM8K +8.0pt
- 单卡可复现（12GB/32GB）；自研受限代码沙箱（超时/资源限制/断网/进程组 kill）
- 工程完整度：断点续训、自动下载、统一评估与汇总

**❌ 证据不足、不对外声明**
- AIME / LiveCodeBench 成绩（前者无区分度，后者未跑）
- ~~"GRPO 无提升"~~ —— 那是 v1 的结论，**v2 已推翻**（见文末附录）

## 7. 复现命令

```bash
# 数学实验
GRPO_CONFIG=configs/grpo_config.server.yaml SFT_N=2500 GRPO_N=4973 GRPO_MAX_STEPS=600 \
  nohup bash run_full_pipeline.sh > logs/full_pipeline.log 2>&1 &

# 代码实验（起点复用数学 SFT 模型）
CODE_MAX_STEPS=300 nohup bash code_rl/run_code_pipeline.sh > code_rl/logs/code.log 2>&1 &
```

---

# 附：v2 改进版结果（2026-09-12）

## v2 改了什么
组大小 `num_generations` 4→8、步数 600/300→2000/800、lr 1e-6→2e-6、beta 0.04→0.02、
奖励从"纯 0/1"升级为**部分分**（数学：正确性+格式+长度；代码：通过/断言失败/语法错分档）。

## v2 结果

| 任务 | Base | SFT | GRPO v1 | **GRPO v2（终版）** | 9B |
|------|------|-----|---------|-------------|-----|
| GSM8K@50 | 30.0% (15) | 34.0% (17) | 34.0% (17) | **50.0% (25)** ✅2000步 | 40.0% (20) |
| HumanEval@164 | 14.6% (24) | 21.9% (36) | 21.9% (36) | **25.0% (41)** ✅800步 | — |
| AIME@60 | 1.7% (1) | 0% | 0% | 0% | 0% |

## v2 结论

### ✅ 两项实验都出现【显著净提升】（v1 是净 0）
- **数学 v2（完整 2000 步 / 22.3 小时）**：GSM8K 34.0% → **50.0%（+16.0pt）**，逐题 **+14 题 / -6 题**
- **代码 v2（完整 800 步）**：HumanEval 21.9% → **25.0%（+3.1pt）**，逐题 **+9 题 / -4 题**
- 逐题"有得有失"说明 RL 确实在改变策略，且这次**得远大于失**——v1 的净 0 是"得=失"。
- ⚠️ 统计说明：GSM8K n=50 → 1σ≈7pt；**+16pt 约 2.3σ，超出噪声**，但若要写进正式材料，建议扩样复核（≥200 题或全量 1319）。

### ✅ 机制被验证：诊断是对的
- `frac_reward_zero_std` 从 v1 的 **0.5 → v2 的 0**：组大小 4→8 后，同组出现"有对有错"，
  优势不再恒为 0 → 每步都有梯度信号。**这直接解释了 v1 为什么净收益为 0。**
- 部分分奖励让组内差异更丰富（训练内 reward 在 0.05~0.97 波动）。

### ⚠️ 需要保留的说明
1. AIME2024 依旧 0/60（对 4B/9B 都太难，无区分度），不能作为结论依据。
2. GSM8K 评测样本 n=50（建议后续扩样以提高置信度）。
3. 数学 v2 已从 checkpoint-30 续训至 2000 步（22.3h），30 步小测的 42% 被终版 50% 取代。

### 与 9B 的关系（核心卖点）
GSM8K 上 **4B-GRPO-v2 50% > 9B-Base 40%（+10pt）**：逐题对比 9B 为 +6/-11（两者各有胜负，
但总体 4B 多答对 5 题）。→ **4B 经 RLVR 后在该任务上超过同代 9B 基线**。

## 下一步（已修复工具链）
- pipeline 已修复：训练判断改为"最新 checkpoint 步数 < 目标步数就继续训"；
  评估判断改为"模型权重比结果新就重评"。→ 续训与重评不会再被跳过。
- 续训命令：`MATH_MAX_STEPS=2000 bash run_both_pipeline.sh`（会从 checkpoint-30 继续）

---

# 附：Skill-RLVR（技能选择）结果 —— 2026-09-14

## 口径
**BFCL v3 分层抽样 400 题**（multiple / live_multiple / simple / irrelevance / live_irrelevance 各 80 题），
贪心解码，指标与 `memoryRL/evaluate_skill.py` 定义一致。训练数据为**自建技能题库**（818 条，24 技能/6 域），
BFCL **只做评测**（泛化测试）。

## 总表
| 策略/模型 | exact | F1 | **误加载率** | 漏加载率 | 平均加载数 |
|-----------|-------|-----|-----------|---------|-----------|
| 全加载 | 0.200 | 0.492 | 100% | 0% | 1.84 |
| 随机 | 0.330 | 0.412 | 100% | 0% | 1.00 |
| **相似度检索（业界现状做法）** | 0.485 | 0.606 | **100%** | 0% | 1.00 |
| 检索+阈值规则 | 0.590 | 0.658 | 65.0% | 15.4% | 0.77 |
| Base（未 RL） | 0.207 | 0.301 | 89.4% | 10.0% | 2.17 |
| start-grpo-v2（只做数学 RL） | 0.160 | 0.311 | 88.1% | 14.2% | 1.97 |
| Skill-RL（起点=Base） | 0.360 | 0.341 | 65.6% | 12.1% | 1.76 |
| **Skill-RL（起点=数学RL模型）** | **0.795** | **0.652** | **22.5%** | **6.7%** | **1.00** |

## 分类别 exact_acc（每类 80 题）
| 类别 | 相似度检索 | 阈值规则 | Base | Skill-RL(Base起点) | **Skill-RL(数学RL起点)** |
|------|-----------|---------|------|------------------|----------------------|
| multiple | 0.887 | 0.863 | 0.013 | 0.200 | **0.850** |
| live_multiple | 0.537 | 0.400 | 0.062 | 0.212 | **0.625** |
| simple | 1.000 | 0.988 | 0.750 | 0.700 | 0.950 |
| **irrelevance（该拒绝）** | **0.000** | 0.325 | 0.150 | 0.388 | **0.650** |
| **live_irrelevance** | **0.000** | 0.375 | 0.062 | 0.300 | **0.900** |

**误加载率（irrelevance / live_irrelevance）**：相似度检索 **100% / 100%** → 阈值规则 67.5% / 62.5%
→ Skill-RL(数学RL起点) **35.0% / 10.0%**。

## 三条结论
1. **技能选择策略全面超过所有基线**：exact **0.795** vs 最佳基线（阈值规则 0.590，**+20.5pt**）、
   vs 相似度检索 0.485（**+31.0pt**）。
2. **直击"加载不准"痛点**：检索方案**永不拒绝**（误加载率 100%）；
   Skill-RL 把误加载率降到 **22.5%**（live_irrelevance 上仅 **10%**），同时漏加载仅 6.7%、平均只加载 1.00 个技能。
3. **跨任务正迁移成立**：`Δ_base = +15.2pt`，`Δ_mathrl = +63.5pt` →
   **迁移效应 = +48.3pt**。即"先用数学 RL 训过的模型"作为技能 RL 的起点，学习效率显著更高。
   ⚠️ 诚实说明：**只做数学 RL 并不会自动提升选择能力**（start-grpo-v2 0.160 < Base 0.207），
   但它是**更好的起点**（可塑性/指令遵循更好），因此 RL 后收益更大。

## 与主项目的关系
三层能力同一套 GRPO+RLVR 流水线：推理 GSM8K 34%→50%（>9B 40%）→ 执行 HumanEval 21.9%→25.0%
→ 选择 BFCL exact 0.795（误加载 22.5%），并证明**层间正迁移**。

## 遗留
- 技能训练日志（reward 曲线/步数）未随产物拷贝；如需训练曲线请带上 `memoryRL/logs/skill.log`。
- 评测为 400 题分层抽样；全量 2771 题评测可进一步收紧置信区间（随时可跑）。
- A2（多轮 load_skill → 用技能解题，接代码沙箱）为下一步。

## 训练动态（两臂各 800 步，LoRA 适配器保存）
| | arm A：起点=Base | arm B：起点=数学RL |
|---|---|---|
| 训练时长 | 338 分钟（5.6h） | 380 分钟（6.3h） |
| 每步耗时 | 25.3s | 28.5s |
| 训练内 reward（前10%→后10%）| 0.492 → **0.984**（Δ+0.492） | 0.332 → **0.962**（Δ+0.630） |
| 零方差步占比 | 0.383 | 0.312 |
| 产物 | `outputs/qwen3.5-4b-grpo-skill-base`（LoRA 适配器）| `outputs/qwen3.5-4b-grpo-skill-mathrl` |
| **BFCL held-out exact** | **0.360** | **0.795** |

训练曲线图：`memoryRL/skill_training_curve.png`

### ⚠️ 一个重要且诚实的观察
**训练内 reward 与 held-out 表现不一致**：
- 两臂在自己的题库（818 条）上最终都冲到 0.96+（接近饱和，约 1 个 epoch），但**只有数学RL 起点那一臂泛化到 BFCL 上（0.795 vs 0.360）**
- 也就是说：**"训练奖励涨得快"不代表"学得对"**；BFCL（自建题库之外的公开基准）才是真信号
- 这正是我们坚持"训练自建 / 评测 BFCL"分离设计的价值：如果直接在 BFCL 上训练，就会把这个泛化信号测没了
