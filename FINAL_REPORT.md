# 项目报告（定稿）：Qwen3.5-4B 的 GRPO + RLVR 三层 Agent 能力训练

> 详细实验过程见 `EXPERIMENT_REPORT.md`；简历/面试材料见 `RESUME.md`；逐日排查记录见 `PROJECT_LOG.md`。
> 环境：V100 32GB（Tesla PG503-216, sm_70）· torch 2.6.0+cu124 · trl 1.12 · transformers 5.16 · 本地另在 RTX 4070 12GB 上验证可复现。

---

## 摘要

用**同一套 TRL GRPO + 可验证奖励（RLVR）流水线**，在 Qwen3.5-4B 上依次训练三层 Agent 能力：

| 能力层 | 任务（可验证奖励） | 基线 → 结果 | 关键结论 |
|--------|------------------|-----------|---------|
| **推理** | GSM8K（答案比对） | 34.0% → **50.0%**（2000 步） | 超过同代 **9B-Base 的 40.0%** |
| **执行** | HumanEval / MBPP（沙箱跑单测）| 21.9% → **25.0%**（800 步） | 自研受限代码沙箱；数学 SFT 跨任务迁移 **+7.3pt** |
| **选择** | BFCL v3（技能集合匹配 + 拒绝）| 0.485(检索) / 0.590(规则) → **0.795** | **误加载率 100% → 22.5%**；跨任务正迁移 **+48.3pt** |

三层共用训练底座（模型加载/冻结视觉塔/LoRA、GRPO 超参、断点续训、评估汇总），**新增一层只需写"题库 + 奖励函数"**。

---

## 1. 背景与动机

- RLVR（可验证奖励强化学习）+ GRPO 已成为 LLM 后训练提升推理能力的主流范式，但公开方案普遍依赖多卡/大模型，**低成本复现困难**
- Agent 侧的痛点是**工具/技能加载不准**：工具数 ≥20 时前沿模型准确率从 95–96% 降到 65–78%，失败分解中**错选占 18%**；且有研究指出模型常"看到了正确工具仍选错"→ 瓶颈在**决策**，不在检索
- 因此本项目回答两个问题：**① 小模型能否用 RL 提升能力并逼近大模型？② 同一套 RLVR 流水线能否覆盖"推理→执行→选择"三层 Agent 能力并互相促进？**

## 2. 方法

**统一流水线**：冷启动 SFT（可选）→ GRPO 在线采样 → 可验证奖励打分 → 组内相对优势更新 → 评估。
**共同零件**：多模态模型正确加载（`AutoModelForImageTextToText` + 冻结视觉塔 + LoRA，12GB 可跑）、断点续训、自动下载与镜像缓存、统一评估与汇总。

| 层 | 题库 | 奖励设计 |
|----|------|---------|
| 推理 | GSM8K train（SFT 段与 RL 段**不相交**）| 答案数值比对 + 格式分 + 长度惩罚 |
| 执行 | MBPP(464) | 沙箱执行单测：通过 1.0 / 断言失败 0.3 / 语法错 0.1 + 格式分 |
| 选择 | 自建技能题库(818，24 技能/6 域，含 near-miss 与"无需技能"样本) | 集合完全匹配 1.0；Jaccard 部分分；**该拒绝却加载 / 该加载却拒绝 = 0**；多加载扣分 |

**防污染设计**：SFT 与 RL 题库不相交；技能选择**训练自建题库、评测公开基准 BFCL**（把 BFCL 当泛化测试）。

## 3. 结果

### 3.1 推理层（GSM8K，随机 50 题）
| 模型 | 正确率 |
|------|--------|
| 4B-Base | 30.0% |
| 4B-SFT | 34.0% |
| 4B-GRPO v1（组大小 4）| 34.0%（净收益≈0）|
| **4B-GRPO v2（组大小 8 + 部分分）** | **50.0%** |
| **9B-Base（参照）** | **40.0%** |

- v2 相对 SFT **+16.0pt**，逐题 **+14 / −6**；训练 2000 步 / 22.3h
- **机制**：v1 有 **50% 的步组内奖励方差为 0**（优势=0，等于白跑）→ 组大小 4→8 + 部分分奖励后**降为 0**，两任务同时转为净提升

### 3.2 执行层（HumanEval，164 题）
| 模型 | pass@1 |
|------|--------|
| 4B-Base | 14.6% |
| 4B-SFT（**数学**冷启动）| 21.9%（跨任务迁移 **+7.3pt**）|
| **4B-Code-GRPO v2** | **25.0%** |

自研受限沙箱：子进程隔离 + 硬超时 + **进程组 kill** + rlimit（CPU/内存/文件/进程数）+ **断网** + 临时目录（挡得住死循环/吃内存，挡不住刻意提权，README 有警示）。

### 3.3 选择层（BFCL v3，分层 400 题：multiple / live_multiple / simple / irrelevance / live_irrelevance 各 80）
| 策略/模型 | exact | **误加载率** | 漏加载率 | 平均加载数 |
|-----------|-------|-----------|---------|-----------|
| 相似度检索（业界现状）| 0.485 | **100%** | 0% | 1.00 |
| 检索 + 阈值规则 | 0.590 | 65.0% | 15.4% | 0.77 |
| Base | 0.207 | 89.4% | 10.0% | 2.17 |
| Skill-RL（起点=Base）| 0.360 | 65.6% | 12.1% | 1.76 |
| **Skill-RL（起点=数学RL）** | **0.795** | **22.5%** | **6.7%** | **1.00** |

- **该拒绝的两类**：`irrelevance` 检索 0.000 → 规则 0.325 → **我们的 0.650**；`live_irrelevance` 检索 0.000 → 规则 0.375 → **我们的 0.900（误加载仅 10%）**
- **跨任务正迁移**：`Δ_base = +15.2pt`，`Δ_mathrl = +63.5pt` → **迁移效应 +48.3pt**
- **训练动态**（两臂各 800 步）：训练内 reward 均升至 0.96+，但**只有数学RL 起点那臂泛化到 BFCL（0.795 vs 0.360）**
  → **"训练奖励 ≠ 泛化能力"**，印证"训练自建 / 评测公开基准"分离设计的必要性。曲线见 `memoryRL/skill_training_curve.png`

## 4. 结论

1. **小模型可通过 RL 超越更大模型**：4B 经 RLVR 后在 GSM8K 上 **50% > 9B-Base 40%**
2. **RL 不涨的根因被定位并修复**：组内零方差步占比 0.5 → 0（组大小 + 部分分奖励），这是本项目最有含量的因果结论
3. **一套流水线可覆盖三层 Agent 能力**：换"题库 + 奖励函数"即可（数学比数字 / 代码跑沙箱 / 技能比集合）
4. **技能加载不准可被 RL 显著改善**：exact 0.795（超最佳基线 +20.5pt），**误加载率 100% → 22.5%**
5. **层间存在正迁移**：推理 RL 训过的模型是更好的"选择策略"起点（迁移效应 +48.3pt）

## 5. 局限（如实）

| 局限 | 说明 | 补强方式 |
|------|------|---------|
| GSM8K n=50（1σ≈7pt）| 建议扩样 | `EVAL_N=200` 或全量 1319 |
| BFCL 分层 400（1σ≈2.5pt）| 建议全量 | 跑全量 2771 题 |
| AIME2024 无区分度 | 4B/9B 均 0~1 题 | 不写入结论 |
| 技能训练题库自建（818）| 规模有限；near-miss 设计可能偏易 | 扩充/引入真实工具文档（MCP 类）|
| 9B 对比仅 GSM8K | 单基准 | 补 AIME/HumanEval 上的 9B 数字 |
| A2（多轮"加载→使用"）未做 | 仅单轮选择 | 接 `code_rl/code_sandbox.py` 做闭环 |

## 6. 复现命令

```bash
# 推理层（数学）
GRPO_CONFIG=configs/grpo_config.v2.yaml MATH_OUT=outputs/qwen3.5-4b-grpo-v2 MATH_TAG=grpo-v2 \
  GRPO_MAX_STEPS=2000 bash run_full_pipeline.sh

# 执行层（代码）
CODE_CONFIG=code_rl/code_config.v2.yaml CODE_OUT=outputs/qwen3.5-4b-grpo-code-v2 \
  CODE_MAX_STEPS=800 bash code_rl/run_code_pipeline.sh

# 选择层（技能）—— 先基线（秒级，无需 GPU）再双臂训练
bash memoryRL/run_skill_pipeline.sh
SKILL_ARMS=mathrl SKILL_MAX_STEPS=800 bash memoryRL/run_skill_pipeline.sh   # 只跑一臂
```
所有 pipeline 均**幂等可断点续跑**（训练按 checkpoint 续、评估按"权重比结果新"重评）。

## 7. 产物清单

| 产物 | 路径 |
|------|------|
| 推理/执行/选择三层模型 | `outputs/qwen3.5-4b-grpo-v2`、`outputs/qwen3.5-4b-grpo-code-v2`、`server2/qwen3.5-4b-grpo-skill-{base,mathrl}`（LoRA 适配器）|
| 评估结果 | `outputs/outputs_serverv2/results/`、`server2/results/bfcl_skill_*.json` |
| 训练日志/曲线 | `logs/`、`code_rl/logs/`、`server2/skill.log`、`memoryRL/skill_training_curve.png` |
| 代码 | `scripts/`（主流水线）、`code_rl/`（代码 RL + 沙箱）、`memoryRL/`（技能选择）|
| 文档 | `FINAL_REPORT.md`（本文）、`EXPERIMENT_REPORT.md`、`RESUME.md`、`PROJECT_LOG.md`、`plan1.md`（选择层方案）|
